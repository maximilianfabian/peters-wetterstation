# Copy this file to ".env.local" and paste your real key after the = sign.
# The file ".env.local" is ignored by git, so your secret key is never committed.
#
# Get a key from https://console.anthropic.com  (and set a hard monthly
# spending cap there first, before anything else).

ANTHROPIC_API_KEY=your-key-goes-here
